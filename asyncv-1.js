const listLocation = (locations) => {
    locations.forEach((location) => {
        // console.log(location)
    })
}

const  myLocations = ['Thailand','China']
listLocation(myLocations)
const ExOnlyKnow = listLocation(myLocations)
console.log(ExOnlyKnow)